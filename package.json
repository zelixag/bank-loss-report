{
  "name": "@xmov/bank-loss-report",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "release": "deploy-oss --build --dist ./dist --path bank-loss-report/",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@xmov/asr": "workspace:*",
    "@xmov/llm": "workspace:*",
    "@xmov/sdk-core": "workspace:*",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@xmov/deploy-oss": "workspace:*",
    "@vitejs/plugin-vue": "^6.0.7",
    "typescript": "~5.7.0",
    "vite": "^8.0.14",
    "vue-tsc": "^3.3.2"
  }
}
